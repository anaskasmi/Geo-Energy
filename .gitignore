# ── Secrets (never commit) ────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── Data artifacts (built by `make ingest`, never tracked) ─────────────────────
/data/
data/
*.duckdb
*.duckdb.wal
*.pmtiles
*.mbtiles
*.tif
*.tiff
*.parquet
*.geoparquet
# but keep tiny fixtures used by tests
!ingest/tests/**/*.parquet
!ingest/tests/**/*.geoparquet
# and keep source-controlled reference data shipped inside the ingest package (e.g. the
# curated zoning_rules.csv, FR-A2) — this is code, not a built artifact.
!ingest/pipeline/data/
!ingest/pipeline/data/**

# ── Python ─────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv*/
venv/
.env-venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ── Node / frontend ──────────────────────────────────────────────────────────
node_modules/
frontend/dist/
frontend/.vite/
*.log
npm-debug.log*

# ── Tooling scratch dirs ───────────────────────────────────────────────────────
.playwright-mcp/

# ── OS / editor cruft ──────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
