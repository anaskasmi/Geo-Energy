# Ingestion pipeline dependencies (GEO-2). DuckDB is pinned (spatial extension ABI);
# fuller pinning/lockfile is GEO-33.
duckdb==1.1.3
pyarrow>=17,<19
pyproj>=3.6,<4
httpx>=0.27,<0.29

# CAISO interconnection queue (GEO-7). Imported lazily, only on the live fetch path; the
# offline build / test path uses a pre-staged CSV (GEO_CAISO_QUEUE_SOURCE) and needs neither
# this nor the network. Pulls pandas. Loose pin here; full pinning/lockfile is GEO-33.
gridstatus>=0.30

# Slope / DEM raster (GEO-9). rasterio (+ numpy) compute slope from the DEM and write the
# slope.tif sidecar — needed on BOTH the live and offline (pre-staged DEM) paths, so they are
# real deps (this is the one fetcher that does raster work; DuckDB spatial is vector-only).
# seamless-3dep fetches USGS 3DEP DEM tiles and is imported lazily on the live path only
# (offline/test path uses GEO_DEM_SOURCE). Loose pins here; full pinning/lockfile is GEO-33.
rasterio>=1.3,<2
numpy>=1.26
seamless-3dep>=0.2

# Dev/test (kept in-image so `make test` can run pytest inside the container).
pytest>=8,<9
