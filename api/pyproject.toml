[project]
name = "geo-api"
version = "0.1.0"
description = "Read-only FastAPI service (Service B) for the site-selection app."
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
